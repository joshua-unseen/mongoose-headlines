<div class="container">
    <div class="jumbotron text-center">
        <h1>Scrape the Register</h1>
        <h5>Biting the hand that bites the hand that feeds IT</h5>
        <h6>or something like that</h6>
    </div>
    <div class="card">
        <div class="card-body">
            {{#if data}}
            <table class="table table-hover">
                <thead>
                    <th colspan="3">
                        Today's Headlines
                    </th>
                </thead>
                <tbody>
                    {{#each data}}
                    <tr id="{{@index}}" data-obj="{{stringObj}}">
                        <td>
                            {{#if image}}
                            <img src="{{image}}" class="img-thumbnail" alt="article thumb">
                            {{else}}
                            <img src="assets/images/default.jpg" class="img-thumbnail" alt="default thumb">
                            {{/if}}
                        </td>
                        <td>
                            <p>{{section}}</p>
                            <a href="{{link}}">{{headline}}</a>
                            <p>{{{summary}}}</p>
                        </td>
                        <td class="align-middle">
                            <button class="btn btn-primary save-btn" data-id="{{@index}}">Save</button>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            {{else}}
            No new articles
            <a href="scrape" class="btn btn-primary" id="scrape-btn">Scrape Headlines</a href="scrape">
            {{/if}}
        </div>
    </div>
</div>